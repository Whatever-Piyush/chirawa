export interface Coordinates {
  lat: number;
  lng: number;
}

export interface AddressSnapshot {
  street: string;
  landmark: string;
  locality: string;
  city: string;
  pincode: string;
  lat: number;
  lng: number;
}