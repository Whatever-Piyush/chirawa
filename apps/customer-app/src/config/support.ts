// Production support contact — founder-operated WhatsApp (A4-impl-1).
// Single source of truth: two audits in a row found screens still dialing a
// placeholder number because each screen kept its own copy. Import this instead.
export const SUPPORT_WHATSAPP_NUMBER = '916350076685';
