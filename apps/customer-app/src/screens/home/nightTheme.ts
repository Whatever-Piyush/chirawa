// Shared night-theme palette used when the store is closed (outside delivery
// hours). The header background and the ClosedBanner card both use this so the
// closed surface reads as one cohesive deep-space sky.
export const NIGHT_FROM = '#0A0E2A'; // deep space navy (top)
export const NIGHT_TO   = '#34245F'; // deep violet (bottom)
